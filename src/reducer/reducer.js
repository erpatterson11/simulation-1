const initialState = {
    shelfA: {
        bin1: null,
        bin2: null,
        bin3: null,
        bin4: null,
        bin5: null
    },
    
}

export default function(state = initialState, action) {
    switch(action.type) {
        default:
            return state
    }
}

